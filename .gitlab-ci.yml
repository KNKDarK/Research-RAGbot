stages:
  - lint
  - format
  - type_check
  - test
  - security
  - secret-detection

include:
  - template: Security/SAST.gitlab-ci.yml
  - template: Security/Secret-Detection.gitlab-ci.yml

variables:
  SECRET_DETECTION_ENABLED: 'true'
  PIP_CACHE_DIR: ${CI_PROJECT_DIR}/.cache/pip

.python-base:
  image: python:3.11-slim
  cache:
    paths:
      - .cache/pip
  before_script:
    - python -m pip install --upgrade pip
    - pip install -r requirements.txt

ruff-lint:
  stage: lint
  extends: .python-base
  script:
    - ruff check .

ruff-format:
  stage: format
  extends: .python-base
  script:
    - ruff format --check .

mypy:
  stage: type_check
  extends: .python-base
  script:
    - mypy . --ignore-missing-imports

pytest:
  stage: test
  extends: .python-base
  coverage: '/TOTAL\s+\d+\s+\d+\s+(\d+%)/'
  script:
    - PYTHONPATH=. pytest --cov=. --cov-report=term-missing --cov-report=xml --cov-fail-under=20
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
    paths:
      - coverage.xml
    expire_in: 1 week

bandit:
  stage: security
  extends: .python-base
  script:
    - bandit -r . -ll -c bandit.yaml

pip-audit:
  stage: security
  extends: .python-base
  script:
    - pip-audit

secret_detection:
  stage: secret-detection
